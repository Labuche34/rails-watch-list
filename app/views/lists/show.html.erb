<div class="card shadow-sm mx-auto" style="max-width: 600px;">
  <div class="card-body">
    <h1 class="card-title mb-3"><%= @list.title %></h1>
    <p class="card-text"><%= @list.details %></p>
    <%= link_to "Back to list", lists_path, class: "btn btn-outline-primary mt-3" %>
  </div>
</div>
