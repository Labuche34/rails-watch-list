<h1>NEW</h1>
<%= form_with model: @list, html: { class: "card p-4 shadow-sm", style: "max-width: 500px; margin: auto;" } do |f| %>
  <div class="mb-3">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <%= f.submit "Enregistrer", class: "btn btn-primary w-100" %>
<% end %>

<div class="text-center mt-3">
  <%= link_to "Retour à la liste", lists_path, class: "btn btn-outline-secondary" %>
</div>
